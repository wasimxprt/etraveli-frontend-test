export default interface MoviesList {
    title: string,
    release_date: string,
    created: string, 
    poster: string
}

export default interface Movie {
    title: string,
    details: string,
}
